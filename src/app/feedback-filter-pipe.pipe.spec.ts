import { FeedbackFilterPipePipe } from './feedback-filter-pipe.pipe';

describe('FeedbackFilterPipePipe', () => {
  it('create an instance', () => {
    const pipe = new FeedbackFilterPipePipe();
    expect(pipe).toBeTruthy();
  });
});
